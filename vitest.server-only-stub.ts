// Stub for the `server-only` package under vitest. Real Next.js builds
// throw if `server-only` is imported in a client bundle; tests don't have
// that boundary so this just resolves to an empty module.
export {};
